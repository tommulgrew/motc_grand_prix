#title Mot Grand Prix
#include "library/trig"
#include "assets"
#include "tracks"
#include "models"
#include "trackdata"

Track[] tracks = new Track[0];

// Deserialize tracks
for (int i = 0; i < trackData.length; i++) {
	tracks.add(loadTrack(new Stream { data = trackData[i] }, trackNames[i]));
}


drawimg(textureImage, 0, 0);
flip();

