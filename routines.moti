Vec skew(Vec pos, float xd, float yd) {
    return new Vec {
        x = pos.x + pos.z * xd,
        y = pos.y + pos.z * yd,
        z = pos.z
    };
}

float lerp(float a, float b, float f) {
    return (1 - f) * a + f * b;
}
